export default function Score() {
    return (
        <div className="score">
            {/* <p>{player.nickname} : {player.score}</p>
            <p>{player.nickname} : {player.score}</p> */}
        </div>
    )
}
